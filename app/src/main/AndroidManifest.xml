<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hr.fer.grupa.erestoran">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MethodSelectActivity"></activity>
        <activity android:name=".HomeActivity"></activity>
        <activity android:name=".SignInActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".Jezik"></activity>
        <activity android:name=".Tutorial5" />
        <activity android:name=".Tutorial4" />
        <activity android:name=".Tutorial3" />
        <activity android:name=".Tutorial1" />
        <activity android:name=".Tutorial2" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>